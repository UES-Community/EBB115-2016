import { Callout } from "./Callout";
import { CodeBlock, Pre, InlineCode } from "./CodeBlock";
import { Sandbox } from "./Sandbox";
import { References, ReferenceItem } from "./Reference";

export const mdxComponents = {
  Callout,
  Sandbox,
  pre: Pre,
  code: InlineCode,
  CodeBlock,
  References,
  ReferenceItem,
};
